# Shared secret for POST /api/ingest (must match the Cloudflare Worker).
INGEST_TOKEN=changeme

# Hospital name substrings to show, separated by ; (commas stay inside names).
HOSPITALS=Christ Hospital, The - Liberty; Kettering Health Hamilton; Atrium; Mercy Health - Fairfield Hospital

# Web.
WEB_HOST=0.0.0.0
WEB_PORT=8080
DATA_DIR=/tmp/juvare-data
WEB_DIR=web